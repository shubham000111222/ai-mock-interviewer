version: "3.9"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./tmp_audio:/app/tmp_audio
    restart: unless-stopped
